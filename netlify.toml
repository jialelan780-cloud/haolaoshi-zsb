# Netlify 部署配置 —— Next.js (App Router)
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # 用 Node 20 LTS 构建（与 Next.js 14 兼容）
  NODE_VERSION = "20"

# Netlify 官方 Next.js 运行时插件（处理 SSR / 路由 / 图片等）
[[plugins]]
  package = "@netlify/plugin-nextjs"
